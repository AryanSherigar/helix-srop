# Reranking Eval (LLM-as-judge)

Run this to generate the before/after results:

```
python -m app.rag.rerank_eval --output docs/reranking-eval.md
```

Queries:
1. How do I rotate a deploy key?
2. How do I configure webhooks and verify signatures?
3. What is the difference between Pro and Enterprise billing plans?
4. How do I set up runners for Helix builds?
5. How do I enable secret scanning for repos?

## Before (Vector Only)

TBD - run the script to populate.

## After (LLM Rerank)

TBD - run the script to populate.
